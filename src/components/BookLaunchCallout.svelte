<aside class="book-callout" aria-labelledby="coding-career-handbook-title">
	<p class="book-status">Open Sourced August 18, 2026</p>
	<h2 id="coding-career-handbook-title">The Coding Career Handbook is now free.</h2>
	<p>
		The complete book is available to read online, and its source is open for everyone to use,
		share, and improve.
	</p>
	<div class="book-actions">
		<a class="primary-action" href="https://learninpublic.org/">Read free</a>
		<a href="https://forge.smol.ai/swyx/codingcareer">View source</a>
	</div>
</aside>

<style>
	.book-callout {
		margin-block: 0.9rem;
		border: 1px solid color-mix(in srgb, var(--page-accent) 48%, var(--page-border));
		border-radius: 0.65rem;
		padding: clamp(1.1rem, 3vw, 1.75rem);
		background:
			linear-gradient(135deg, var(--page-accent-soft), transparent 68%), var(--page-surface);
		box-shadow: 0 12px 34px color-mix(in srgb, var(--page-accent) 10%, transparent);
	}

	.book-status {
		margin: 0 0 0.45rem;
		color: var(--page-accent);
		font-family: var(--font-mono);
		font-size: 0.76rem;
		font-weight: 800;
		letter-spacing: 0.06em;
		text-transform: uppercase;
	}

	h2 {
		margin: 0;
		font-size: clamp(1.45rem, 4vw, 2rem);
		line-height: 1.1;
		letter-spacing: -0.025em;
	}

	.book-callout > p:not(.book-status) {
		max-width: 46rem;
		margin: 0.65rem 0 0;
		color: var(--page-muted);
	}

	.book-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.65rem;
		margin-top: 1rem;
	}

	.book-actions a {
		border: 1px solid var(--page-border);
		border-radius: 0.45rem;
		padding: 0.45rem 0.75rem;
		background: var(--page-surface);
		color: var(--page-text);
		font-weight: 750;
		text-decoration: none;
	}

	.book-actions a:hover {
		border-color: var(--page-accent);
		color: var(--page-accent);
	}

	.book-actions .primary-action {
		border-color: var(--page-accent);
		background: var(--page-accent);
		color: white;
	}

	.book-actions .primary-action:hover {
		background: color-mix(in srgb, var(--page-accent) 88%, black);
		color: white;
	}
</style>
